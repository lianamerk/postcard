# build output
dist/
.output/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS-specific
.DS_Store

# metadata (generated)
src/data/metadata.json

# Category folders in root directory (images are in public/ instead)
# These are optional source folders - images should go directly in public/
# Use wildcard to ignore any category folder in root
# (but allow public/ folders to be tracked)
/[A-Z]*/
/[A-Z]*_*/
/[A-Z]*-*/

# Exclude thumbnail directories (not used, generated during build if needed)
public/*/thumbs/

# Documentation files (reference/notes - not needed in repo)
PERFORMANCE_OPTIMIZATION.md
CLOUDINARY_VS_CLOUDFLARE.md
WORKFLOW.md
DEPLOYMENT.md
ENABLE_PAGES.md
CUSTOM_DOMAIN_SETUP.md
